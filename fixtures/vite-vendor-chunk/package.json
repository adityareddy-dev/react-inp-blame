{
  "name": "vite-vendor-chunk-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "fixtures/vite-react-ts moved to Vite 7 (Rollup), @vitejs/plugin-react 5 and React 18.3, with a manualChunks rule that sends node_modules to one vendor chunk, the setup the README said put react-dom ahead of the install. Run by scripts/vite-app.mjs --fixture vite-vendor-chunk from a temp copy.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-inp-blame": "^0.3.0"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "@types/node": "^24.13.3",
    "@types/react": "^18.3.28",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^5.2.0",
    "typescript": "~6.0.2",
    "vite": "^7.3.6"
  }
}
